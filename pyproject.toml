# This project was generated with 0.4.1 using template: https://github.com/beeware/briefcase-template @ v0.4.1
[tool.briefcase]
project_name = "arsenal"
bundle = "com.arsenal"
version = "2.1.0"
url = "https://github.com/vshadrov/arsenal"
license.file = "LICENSE"
author = "Василий Шадров"
author_email = "shadrovv@gmail.com"

[tool.briefcase.app.arsenal]
formal_name = "Арсенал"
description = "МОДРОП Арсенал"
long_description = "Методика Арсенал для оценки динамики риска опасного поведения и совершения повторных общественно опасных деяний пациентами с психическими расстройствами, в первую очередь - проходящими принудительное лечение и госпитализированными в недобровольном порядке. Основным фокусом внимания методики является выявление мишеней для терапевтического воздействия и отслеживание динамики тех изменений, которых удалось добиться в ходе этого воздействия."
sources = [
    "src/arsenal",
]
test_sources = [
    "tests",
]
console_app = "arsenal.app:main"

requires = [
    "textual", 
]
test_requires = [
    "pytest",
]
icon = "resources/arsenal"

[tool.briefcase.app.arsenal.macOS]
icon = "resources/arsenal"
universal_build = true
requires = [
    # Add your macOS-specific app requirements here
]
terminal = true

[tool.briefcase.app.arsenal.linux]
icon = "resources/images/arsenal"
categories = ["Office"]
terminal = true

[tool.briefcase.app.arsenal.linux.appimage]
terminal = true

[tool.briefcase.app.arsenal.linux.system.debian]
icon = "resources/images/arsenal"
system_requires = [
    # Add any system packages needed at build the app here
]

system_runtime_requires = [
    # Add any system packages needed at runtime here
]
categories = ["Office"]

[tool.briefcase.app.arsenal.linux.system.rhel]
icon = "resources/images/arsenal"
system_requires = [
    # Add any system packages needed at build the app here
]

system_runtime_requires = [
    # Add any system packages needed at runtime here
]
categories = ["Office"]

[tool.briefcase.app.arsenal.linux.system.suse]
icon = "resources/images/arsenal"
system_requires = [
    # Add any system packages needed at build the app here
]

system_runtime_requires = [
    # Add any system packages needed at runtime here
]
categories = ["Office"]

[tool.briefcase.app.arsenal.linux.system.arch]
icon = "resources/images/arsenal"
system_requires = [
    # Add any system packages needed at build the app here
]

system_runtime_requires = [
    # Add any system packages needed at runtime here
]
categories = ["Office"]

[tool.briefcase.app.arsenal.linux.flatpak]
icon = "resources/images/arsenal"
flatpak_runtime = "org.freedesktop.Platform"
flatpak_runtime_version = "25.08"
flatpak_sdk = "org.freedesktop.Sdk"
categories = ["Office"]

[tool.briefcase.app.arsenal.windows]
icon = "resources/arsenal"
requires = [
    # Add your Windows-specific app requirements here
]
console = true
categories = ["Office"]

# Mobile deployments
[tool.briefcase.app.arsenal.iOS]
supported = false

[tool.briefcase.app.arsenal.android]
supported = false

# Web deployments
[tool.briefcase.app.arsenal.web]
supported = false

